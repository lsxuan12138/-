<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hitfriend.dc.mapper.MediaMapper">
   <insert id="insert">
       insert into tb_file(file_desc, url, create_by, type)
       values (#{fileDesc}, #{url},#{user.id},#{type})
   </insert>
</mapper>